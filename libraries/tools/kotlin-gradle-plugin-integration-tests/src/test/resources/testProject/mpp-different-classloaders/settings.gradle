pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}

enableFeaturePreview('GRADLE_METADATA')

include(":mpp-lib", ":jvm-app", ":js-app")